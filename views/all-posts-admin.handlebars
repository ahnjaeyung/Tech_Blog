{{#each posts as |post|}}
<div>
    <a href="/dashboard/post/{{post.id}}">
        <div class="card">
            <h2>{{post.title}}</h2>
            <p>{{post.createdAt}}</p>
        </div>
    </a>
</div>
{{/each}}