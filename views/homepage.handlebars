<article>
    {{#each posts as |post|}}
    <div>
        <h2>{{post.title}}</h2>
        <p> {{post.createdAt}} </p>
        {{#if post.user.username}}
        <p> {{post.user.username}} </p>
        {{/if}}
    </div>
    {{/each}}
</article>