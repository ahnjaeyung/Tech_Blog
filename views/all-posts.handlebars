<section>
    {{#each posts as |post|}}
    <div>
        <div>
            <h2> {{post.title}} </h2>
            <p> {{post.createdAt}} </p>
        </div>
        <div>
            <p> {{post.body}} </p>
        </div>
    </div>
    {{/each}}
</section>